//In this queries, I'm using a Superstore database to answer questions about the data.//

#1. Order data by lowest to highest price.
SELECT *
FROM superstore
ORDER BY price ASC;

#2. What is the average price of the items?
SELECT AVG(price)
FROM superstore;

#3. What is the sum of the items in the Kitchen Supplies category?
SELECT SUM(price), category
FROM superstore
WHERE category = 'Kitchen Supplies';
